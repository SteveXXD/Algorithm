"""

看上面输出，这个例子就是完整的一维 01 背包。核心就 3 行：
dp = [0]*(C+1)
for w, v in items:
    for j in range(C, w-1, -1):      # j 倒序
        dp[j] = max(dp[j], dp[j-w] + v)
dp[j] 的含义：处理完当前物品后，"容量 j 能装的最大价值"。
逐行看输出：
处理物品1 (w=2,v=3) 后：dp[2..6]全是 3——容量 ≥2 都能装下它
处理物品2 (w=3,v=4) 后：dp[5]=7——这是 dp[2]+4，即"物品1+物品2"（2+3=5 容量，3+4=7 价值）
处理物品3 (w=4,v=5) 后：dp[6]=8——dp[2]+5，即"物品1+物品3"（2+4=6 容量，价值 8）
倒序的作用就在 dp[j-w]这一步：它读到的永远是上一轮（没装过当前物品）的值，所以每个物品最多被装一次。
现在你自己把这 3 行代码敲一遍，跑一遍，把输出和上面对比。跑通了告诉我，我们再进月饼——到时候排序和截止的坑一起讲。

"""


n,T = map(int,input().split())
t = []
b = []
h = []
mc = []
dp = [0] * (T+1)
for _ in range(n):
    ti,bi,hi = map(int,input().split())
    mc.append((ti,bi,hi))
    mc.sort(key=lambda x:x[1])

print(mc)